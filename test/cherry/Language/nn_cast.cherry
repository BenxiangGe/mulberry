# RUN: cherry-driver -dump=mlir %s 2>&1 | FileCheck %s --check-prefix=MLIR

fn main(): UInt64 {
  var t: UInt64[3] = [1, 2, 3];
  var x: UInt64 = t[1];

  x
}

# MLIR: mulberry.tensor.load
# MLIR-SAME: !mulberry.tensor<3xi64>
# MLIR-NOT: cherry_nn.cast
# MLIR-NOT: cir.
