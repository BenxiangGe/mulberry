# RUN: cherry-driver -dump=ast %s 2>&1 | FileCheck %s --check-prefix=AST
# RUN: cherry-driver -dump=mlir %s 2>&1 | FileCheck %s --check-prefix=MLIR

comptime RankedBox<T, Rank: UInt64> = struct {
  value: T
};

fn main(): UInt64 {
  var box: Ptr<RankedBox<UInt64, 2>> =
      heap.alloc<RankedBox<UInt64, 2>>();
  box.value = 42;
  io.print(box.value);
  0
}

# AST: ComptimeTypeAliasDecl {{.*}}name=RankedBox parameter=T, Rank: UInt64 type=struct {value: T}
# AST: VariableStat (id=box
# AST-SAME: type=Ptr<RankedBox<UInt64, 2>>

# MLIR-LABEL: func.func @main()
# MLIR: mulberry.heap.alloc !mulberry.record<RankedBox__UInt64__2
# MLIR: mulberry.record.get_field {{.*}}["value"]
