# RUN: mulberry-driver -dump=mlir %s 2>&1 | FileCheck %s

fn sizeOf(values: List<UInt64>): UInt64 {
  return values.size();
}

fn main(): UInt64 {
  var values: List<UInt64> = list.from([1, 2, 3]);
  return sizeOf(values);
}

# CHECK-LABEL: func.func @sizeOf
# CHECK: return
