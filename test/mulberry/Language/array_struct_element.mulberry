# RUN: mulberry-driver -typecheck %s
# RUN: mulberry-driver %s 2>&1 | FileCheck %s

struct Point {
  x: UInt64,
  y: UInt64
}

fn main(): UInt64 {
  var points: Point[2] = [Point { 1, 2 }, Point { 3, 4 }];
  points[1].x = 40;
  io.print(points[0].x + points[1].x);
  0
}

# CHECK: 41
