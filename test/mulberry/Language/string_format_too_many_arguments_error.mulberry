# RUN: not mulberry-driver -typecheck %s 2>&1 | FileCheck %s

import string;

fn main(): UInt64 {
  const text = string.format("{}", 1, 2);
  return 0;
}

# CHECK: error: too many arguments for format string
