# RUN: not mulberry-driver -typecheck %s 2>&1 | FileCheck %s

fn bad<Args...: Show>(value: UInt64): String {
  return "bad";
}

fn main(): UInt64 {
  return 0;
}

# CHECK: error: a type pack requires a corresponding value pack parameter
