# RUN: not mulberry-driver -typecheck %s 2>&1 | FileCheck %s

fn bad<Args...>(args: Args...): UInt64 {
  const value = args;
  return 0;
}

fn main(): UInt64 {
  return bad(1, true);
}

# CHECK: error: a value pack cannot be used as a runtime value
