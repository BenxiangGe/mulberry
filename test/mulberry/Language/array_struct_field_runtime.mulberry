# RUN: mulberry-driver -typecheck %s
# RUN: mulberry-driver %s 2>&1 | FileCheck %s

struct Box {
  data: UInt64[3]
}

fn main(): UInt64 {
  var box = Box { [1, 2, 3] };
  box.data[1] = 40;
  io.println(box.data[0] + box.data[1] + box.data[2]);
  return 0;
}

# CHECK: 44
