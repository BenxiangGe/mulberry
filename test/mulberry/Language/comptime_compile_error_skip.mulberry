# RUN: mulberry-driver %s 2>&1 | FileCheck %s

fn skip(comptime label: String): UInt64 {
  if false {
    #compileError("this branch must not be evaluated");
  }
  return 7;
}

fn main(): UInt64 {
  io.println(skip("test"));
  return 0;
}

# CHECK: 7
