# RUN: not mulberry-driver -typecheck %s 2>&1 | FileCheck %s

fn echo(comptime text: String): String {
  return text;
}

fn main(): UInt64 {
  var text = "hello";
  io.println(echo(text));
  return 0;
}

# CHECK: error: expected a comptime value
