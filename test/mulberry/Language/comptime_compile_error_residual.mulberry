# RUN: not mulberry-driver -typecheck %s 2>&1 | FileCheck %s

fn fail(message: String): UInt64 {
  #compileError(message);
  return 0;
}

fn main(): UInt64 {
  var message = "runtime";
  return fail(message);
}

# CHECK: error: compileError requires a comptime String
