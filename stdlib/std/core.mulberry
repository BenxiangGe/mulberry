package std.core;

comptime Char = UInt8;

fn identity(value: UInt64): UInt64 {
  return value;
}

fn boolToUInt64(value: Bool): UInt64 {
  var result = 0;
  if value {
    result = 1;
  }
  return result;
}
